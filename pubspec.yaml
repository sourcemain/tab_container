name: tab_container
description: Animated and customisable tab view widget.
version: 0.0.1+2
homepage: https://github.com/sourcemain/tab_container
repository: https://github.com/sourcemain/tab_container

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
